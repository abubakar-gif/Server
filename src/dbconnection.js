/*const { createConnection } = require('mysql2');

const dbconnection = createConnection({
  host: "localhost",
  user: "root",
  password: "",
  database: "best_deals",
  connectTimeout: 1000,
});

module.exports = {
  dbconnection,
};*/
